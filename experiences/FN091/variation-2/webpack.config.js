const path = require('path');
const baseConfig = require('../../../config/webpack.experience.js');

module.exports = {
  ...baseConfig('FN091', 'variation-2'),
  // Override base config here if needed
};
